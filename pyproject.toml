[project]
name = "host-intrusion-detector"
version = "0.1.0"
description = "Lightweight host intrusion detection using Windows Event Logs / Sysmon."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Joey Wang" }]
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7"]

[project.scripts]
hids = "hids.cli:main"

[tool.pytest.ini_options]
pythonpath = ["."]
